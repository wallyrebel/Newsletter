# Email Configuration (Required)
EMAIL_TO=recipient@example.com
EMAIL_FROM=digest@example.com

# SMTP Configuration (Required)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password

# Optional: SendGrid (alternative to SMTP)
# SENDGRID_API_KEY=your_sendgrid_key

# Optional: Mailgun (alternative to SMTP)
# MAILGUN_API_KEY=your_mailgun_key
# MAILGUN_DOMAIN=your_domain.mailgun.org

# Holiday APIs (Optional - for fun observances)
# Get free key at: https://calendarific.com/
CALENDARIFIC_API_KEY=

# Alternative holiday API
# Get key at: https://www.checkiday.com/api
CHECKIDAY_API_KEY=

# National News (Optional)
# Default uses Google News RSS (no key required)
# If you want NewsAPI: https://newsapi.org/
NEWS_API_KEY=

# Gas Prices (Optional)
# If using a paid gas price API
GAS_API_KEY=

# Database path (Optional - defaults to data/newsletter.db)
DATABASE_PATH=data/newsletter.db

# Timezone (Optional - defaults to America/Chicago)
TIMEZONE=America/Chicago

# Run hour (Optional - defaults to 19 for 7 PM)
RUN_HOUR=19
