<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>MNG Daily Digest — {{ date_str }}</title>
    <style>
        /* Reset and base styles */
        body, table, td, p, a, li, blockquote {
            -webkit-text-size-adjust: 100%;
            -ms-text-size-adjust: 100%;
        }
        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            font-size: 16px;
            line-height: 1.6;
            color: #333333;
            background-color: #f4f4f4;
        }
        table {
            border-collapse: collapse;
            mso-table-lspace: 0pt;
            mso-table-rspace: 0pt;
        }
        img {
            border: 0;
            height: auto;
            line-height: 100%;
            outline: none;
            text-decoration: none;
            -ms-interpolation-mode: bicubic;
        }
        a {
            color: #1a73e8;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
        
        /* Container */
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
        }
        
        /* Header */
        .header {
            background: linear-gradient(135deg, #1a237e 0%, #283593 100%);
            color: #ffffff;
            padding: 30px 20px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 700;
            letter-spacing: -0.5px;
        }
        .header .date {
            margin-top: 8px;
            font-size: 14px;
            color: rgba(255, 255, 255, 0.9);
        }
        
        /* Section styles */
        .section {
            padding: 25px 20px;
            border-bottom: 1px solid #e0e0e0;
        }
        .section:last-child {
            border-bottom: none;
        }
        .section-title {
            font-size: 20px;
            font-weight: 700;
            color: #1a237e;
            margin: 0 0 20px 0;
            padding-bottom: 10px;
            border-bottom: 2px solid #1a237e;
        }
        .subsection-title {
            font-size: 16px;
            font-weight: 600;
            color: #333;
            margin: 15px 0 10px 0;
        }
        
        /* Article cards */
        .article-card {
            display: block;
            margin-bottom: 20px;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            overflow: hidden;
            background-color: #ffffff;
        }
        .article-image {
            width: 100%;
            height: 180px;
            object-fit: cover;
            display: block;
        }
        .article-content {
            padding: 15px;
        }
        .article-title {
            font-size: 17px;
            font-weight: 600;
            color: #1a237e;
            margin: 0 0 8px 0;
            line-height: 1.3;
        }
        .article-title a {
            color: inherit;
        }
        .article-description {
            font-size: 14px;
            color: #555;
            margin: 0 0 10px 0;
        }
        .article-meta {
            font-size: 12px;
            color: #888;
        }
        
        /* National news */
        .national-item {
            margin-bottom: 15px;
            padding-bottom: 15px;
            border-bottom: 1px solid #eee;
        }
        .national-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        .national-title {
            font-size: 16px;
            font-weight: 600;
            margin: 0 0 5px 0;
        }
        .national-source {
            font-size: 13px;
            color: #666;
        }
        
        /* Weather */
        .weather-card {
            background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 15px;
        }
        .weather-location {
            font-size: 16px;
            font-weight: 600;
            color: #1565c0;
            margin: 0 0 8px 0;
        }
        .weather-temps {
            font-size: 24px;
            font-weight: 700;
            color: #333;
            margin: 0 0 5px 0;
        }
        .weather-temps .high {
            color: #d32f2f;
        }
        .weather-temps .low {
            color: #1976d2;
        }
        .weather-summary {
            font-size: 14px;
            color: #555;
            margin: 0;
        }
        .weather-precip {
            font-size: 13px;
            color: #1976d2;
            margin-top: 5px;
        }
        
        /* Gas prices */
        .gas-table {
            width: 100%;
            border-collapse: collapse;
        }
        .gas-table th, .gas-table td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #eee;
        }
        .gas-table th {
            background-color: #f5f5f5;
            font-weight: 600;
        }
        .gas-price {
            font-weight: 600;
            color: #2e7d32;
        }
        .gas-note {
            font-size: 13px;
            color: #666;
            font-style: italic;
            margin-top: 10px;
        }
        
        /* Holidays */
        .holiday-list {
            margin: 0;
            padding: 0;
            list-style: none;
        }
        .holiday-list li {
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }
        .holiday-list li:last-child {
            border-bottom: none;
        }
        .holiday-icon {
            margin-right: 8px;
        }
        .no-holidays {
            color: #666;
            font-style: italic;
        }
        
        /* History */
        .history-list {
            margin: 0;
            padding: 0;
            list-style: none;
        }
        .history-list li {
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }
        .history-list li:last-child {
            border-bottom: none;
        }
        .history-year {
            display: inline-block;
            font-weight: 700;
            color: #1a237e;
            min-width: 50px;
        }
        
        /* Footer */
        .footer {
            background-color: #f5f5f5;
            padding: 20px;
            text-align: center;
            font-size: 12px;
            color: #666;
        }
        .footer p {
            margin: 5px 0;
        }
        .footer a {
            color: #1a73e8;
        }
        .source-links {
            margin-top: 10px;
        }
        
        /* Responsive */
        @media only screen and (max-width: 480px) {
            .header h1 {
                font-size: 24px;
            }
            .section {
                padding: 20px 15px;
            }
            .article-image {
                height: 150px;
            }
            .weather-temps {
                font-size: 20px;
            }
        }
    </style>
</head>
<body>
    <center>
        <table role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%">
            <tr>
                <td style="padding: 20px 0;">
                    <table role="presentation" cellspacing="0" cellpadding="0" border="0" class="email-container" width="600">
                        
                        <!-- Header -->
                        <tr>
                            <td class="header">
                                <h1>Mississippi News Group</h1>
                                <h1 style="font-size: 22px; font-weight: 400;">Daily Digest</h1>
                                <p class="date">{{ date_display }}</p>
                            </td>
                        </tr>
                        
                        <!-- Local News Section -->
                        <tr>
                            <td class="section">
                                <h2 class="section-title">📰 Local News Digest</h2>
                                
                                {% if articles %}
                                {% for article in articles %}
                                <div class="article-card">
                                    {% if article.image_url %}
                                    <a href="{{ article.url }}">
                                        <img src="{{ article.image_url }}" alt="{{ article.title }}" class="article-image" onerror="this.style.display='none'">
                                    </a>
                                    {% endif %}
                                    <div class="article-content">
                                        <h3 class="article-title">
                                            <a href="{{ article.url }}">{{ article.title }}</a>
                                        </h3>
                                        {% if article.description and article.description != article.title %}
                                        <p class="article-description">{{ article.description }}</p>
                                        {% endif %}
                                        <p class="article-meta">{{ article.source_name }}</p>
                                    </div>
                                </div>
                                {% endfor %}
                                {% else %}
                                <p class="no-holidays">No local articles found in the last 24 hours.</p>
                                {% endif %}
                            </td>
                        </tr>
                        
                        <!-- National News Section -->
                        <tr>
                            <td class="section">
                                <h2 class="section-title">🌐 Major National News</h2>
                                
                                {% if national_news %}
                                {% for headline in national_news %}
                                <div class="national-item">
                                    <h3 class="national-title">
                                        <a href="{{ headline.url }}">{{ headline.title }}</a>
                                    </h3>
                                    <p class="national-source">Source: {{ headline.source }}</p>
                                </div>
                                {% endfor %}
                                {% else %}
                                <p class="no-holidays">National news temporarily unavailable.</p>
                                {% endif %}
                            </td>
                        </tr>
                        
                        <!-- Weather Section -->
                        <tr>
                            <td class="section">
                                <h2 class="section-title">🌤️ Weather Outlook</h2>
                                
                                {% for forecast in weather_forecasts %}
                                <div class="weather-card">
                                    <h3 class="weather-location">{{ forecast.location_name }}</h3>
                                    <p class="weather-temps">
                                        {% if forecast.high_temp is not none %}
                                        <span class="high">{{ forecast.high_temp }}°</span>
                                        {% endif %}
                                        {% if forecast.high_temp is not none and forecast.low_temp is not none %} / {% endif %}
                                        {% if forecast.low_temp is not none %}
                                        <span class="low">{{ forecast.low_temp }}°</span>
                                        {% endif %}
                                        {% if forecast.high_temp is none and forecast.low_temp is none %}
                                        <span style="font-size: 16px;">Temperature unavailable</span>
                                        {% endif %}
                                    </p>
                                    <p class="weather-summary">{{ forecast.summary }}</p>
                                    {% if forecast.precip_chance is not none and forecast.precip_chance > 0 %}
                                    <p class="weather-precip">💧 {{ forecast.precip_chance }}% chance of precipitation</p>
                                    {% endif %}
                                </div>
                                {% endfor %}
                                
                                <p style="font-size: 12px; color: #666; margin-top: 10px;">
                                    <a href="{{ weather_source_url }}">View full forecast at weather.gov →</a>
                                </p>
                            </td>
                        </tr>
                        
                        <!-- Gas Prices Section -->
                        <tr>
                            <td class="section">
                                <h2 class="section-title">⛽ Gas Prices</h2>
                                
                                {% if gas_prices %}
                                <table class="gas-table">
                                    <thead>
                                        <tr>
                                            <th>Location</th>
                                            <th>Regular</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {% for price in gas_prices %}
                                        <tr>
                                            <td>{{ price.location }}</td>
                                            <td class="gas-price">
                                                {% if price.regular %}
                                                ${{ "%.2f"|format(price.regular) }}
                                                {% else %}
                                                N/A
                                                {% endif %}
                                            </td>
                                        </tr>
                                        {% endfor %}
                                    </tbody>
                                </table>
                                {% endif %}
                                
                                {% if gas_status_message %}
                                <p class="gas-note">{{ gas_status_message }}</p>
                                {% endif %}
                                
                                <p style="font-size: 12px; color: #666; margin-top: 10px;">
                                    <a href="{{ gas_source_url }}">View current prices at AAA →</a>
                                </p>
                            </td>
                        </tr>
                        
                        <!-- Holidays Section -->
                        <tr>
                            <td class="section">
                                <h2 class="section-title">🎉 Today's Holidays & Observances</h2>
                                
                                <h4 class="subsection-title">Official Holidays</h4>
                                {% if holidays.major_holidays %}
                                <ul class="holiday-list">
                                    {% for holiday in holidays.major_holidays %}
                                    <li><span class="holiday-icon">🏛️</span>{{ holiday }}</li>
                                    {% endfor %}
                                </ul>
                                {% else %}
                                <p class="no-holidays">No official public holidays today.</p>
                                {% endif %}
                                
                                <h4 class="subsection-title">Fun Observances</h4>
                                {% if holidays.fun_observances %}
                                <ul class="holiday-list">
                                    {% for obs in holidays.fun_observances %}
                                    <li><span class="holiday-icon">🎈</span>{{ obs }}</li>
                                    {% endfor %}
                                </ul>
                                {% elif holidays.error_message %}
                                <p class="no-holidays">{{ holidays.error_message }}</p>
                                {% else %}
                                <p class="no-holidays">No fun observances found for today.</p>
                                {% endif %}
                                
                                {% if holidays.source_links %}
                                <p style="font-size: 12px; color: #666; margin-top: 10px;">
                                    Sources: 
                                    {% for link in holidays.source_links %}
                                    <a href="{{ link }}">{{ link | replace('https://', '') | replace('http://', '') }}</a>{% if not loop.last %}, {% endif %}
                                    {% endfor %}
                                </p>
                                {% endif %}
                            </td>
                        </tr>
                        
                        <!-- This Day in History Section -->
                        <tr>
                            <td class="section">
                                <h2 class="section-title">📜 This Day in History — {{ history_date }}</h2>
                                
                                {% if history_events %}
                                <ul class="history-list">
                                    {% for event in history_events %}
                                    <li>
                                        <span class="history-year">{{ event.year }}</span>
                                        {{ event.event }}
                                    </li>
                                    {% endfor %}
                                </ul>
                                {% else %}
                                <p class="no-holidays">Historical events temporarily unavailable.</p>
                                {% endif %}
                                
                                <p style="font-size: 12px; color: #666; margin-top: 10px;">
                                    <a href="{{ history_source_url }}">Read more on Wikipedia →</a>
                                </p>
                            </td>
                        </tr>
                        
                        <!-- Footer -->
                        <tr>
                            <td class="footer">
                                <p><strong>Mississippi News Group Daily Digest</strong></p>
                                <p>Generated on {{ generated_timestamp }}</p>
                                <p style="margin-top: 10px;">
                                    This is an automated digest. Headlines and links only — no full articles.
                                </p>
                                <div class="source-links">
                                    <p>
                                        Weather: <a href="{{ weather_source_url }}">NWS</a> |
                                        History: <a href="{{ history_source_url }}">Wikipedia</a>
                                    </p>
                                </div>
                            </td>
                        </tr>
                        
                    </table>
                </td>
            </tr>
        </table>
    </center>
</body>
</html>
